# draw a square

import turtle as t

length = 100

t.forward(length)
t.left(120)
t.forward(length * 2)
t.right(120)
t.forward(length)
t.right(120)
t.forward(length * 2)

t.exitonclick()
